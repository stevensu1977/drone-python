kind: pipeline
name: default

steps:
- name: web-flask
  image: python
  commands:
  - pip install -r requirements.txt

- name: publish
  image: plugins/docker
  settings:
      auto_tag: true
      repo: foo/bar
